<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List authors</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>
<body>
    <h1>List authors</h1>
    <table border="1">
        <tr>
            <th>Username</th>
            <th>Email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>

        {% for a in Author11 %}
            <tr>
                <td>{{ a.username }}</td>
                <td>{{ a.email }}</td>
                <td><a href="{{ path('app_edit', {'id': a.id}) }}">Edit</a></td>
                <td><a href="{{ path('app_delete', {'id': a.id}) }}">Delete</a></td>
            </tr>
        {% endfor %}
    </table>
    <td><a href="{{ path('app_Add') }}">Add</a></td>
  
</body>
</html>
